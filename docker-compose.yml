services:
  api:
    build: .
    restart: always
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
      HOST: 0.0.0.0
      TABLES_CACHE_PATH: /app/data/tables-cache.json
      SUBSCRIPTIONS_DB_PATH: /app/data/subscriptions.db
    volumes:
      - api-data:/app/data

volumes:
  api-data:
